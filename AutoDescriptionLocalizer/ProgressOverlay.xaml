<Window x:Class="AutoDescriptionLocalizer.ProgressOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="260" Height="60"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="False"
        ResizeMode="NoResize"
        Focusable="False">
    <Border x:Name="RootBorder"
            CornerRadius="6"
            Background="#DD111111"
            Padding="8"
            SnapsToDevicePixels="True"
            Opacity="0.0"
            >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Grid.Column="0">
                <TextBlock x:Name="StatusText"
                           Text="Preparando..."
                           FontSize="12"
                           Foreground="White"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="180" />
                <ProgressBar x:Name="Pb" Height="8" Minimum="0" Maximum="100" Value="0" Margin="0,6,0,0" />
            </StackPanel>

        </Grid>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.22"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
